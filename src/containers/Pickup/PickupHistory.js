import React, {Component} from 'react'

class PickupHistory extends Component {
  constructor (props) {
    super(props)
    this.state = {
    }
  }

  render () {
    return (
      <div>
        add pick up
      </div>
    )
  }
}

export default PickupHistory
