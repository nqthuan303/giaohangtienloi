import React, {Component} from 'react'

import './styles.css'

export default class ClientInfo extends Component {
  constructor (props) {
    super(props)
    this.state = {
    }
  }

  static propTypes = {
  }

  render () {
    return (
      <div>
        test
      </div>
    )
  }
}
